<!DOCTYPE html>
<html>
  <head>
    <title>Guild Market</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
   <nav class="navbar navbar-expand navbar-light bg-esty" style="10rem;">
    <div class="container-fluid">
      <a class="navbar-brand text-light" style="margin-left:20px;" href="/">Guild Market</a>
      <div class="navbar-nav">
        <a class="nav-link text-light" <%= link_to "Admin Dashboard", "/admin" %></a>
      </div>
    </div>
   </nav>

   <!-- This container envelops everything within a page and applies padding -->
   <div class="container-fluid p-4 footer">
     <% flash.each do |type, msg| %>
       <div id="alert" class="warning" style="position: relative; top: -50%"><%= msg %></div>
     <% end %>
     <%= yield %>
   </div>

   <div class="d-flex justify-content-around bg-dark mt-4">
     <%= render partial: "partials/github_stats" %>
   </div>
  </body>
</html>
